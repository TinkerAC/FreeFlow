% 第五章 系统核心模块实现

\chapter{系统核心模块实现}


\section{Profile与SIWE登录模块实现}

系统首先通过Profile引导窗口连接用户钱包。后端生成nonce后，桌面端构造SIWE消息并请求钱包签名，后端对签名消息进行解析和验签。验证通过后，系统创建或更新用户、钱包身份和登录会话，并将会话token同步到桌面端Web2.5客户端。

该模块的核心安全边界在后端。桌面端只负责发起钱包连接、展示签名内容和保存会话token，后端负责校验nonce是否过期或已消费、签名地址是否与请求地址一致、chainId是否符合要求，以及domain、uri和issuedAt等SIWE字段是否有效。验证成功后，会话以token哈希形式保存到\texttt{auth\_sessions}表，桌面端后续访问受保护接口时通过Authorization头携带会话token。


\section{创作者工作台模块实现}

Creators Workshop是创作者进行链上音乐发布的主要入口。该模块支持发行项目列表、草稿创建、作品信息编辑、素材状态展示、metadata上传、链上发布和访问查询。系统通过Redux状态管理工作台内部状态，并通过Web2.5 API与后端同步发行草稿。

发行状态主要包括DRAFT、ASSETS\_PENDING、ASSETS\_UPLOADED、METADATA\_UPLOADED、PUBLISHING、PUBLISHED和FAILED。每个状态对应不同的操作权限和界面提示。

发行状态流转如图\ref{fig:release_state_flow}所示。工作台通过状态字段限制用户操作顺序，避免在素材未上传、metadata未生成或链上交易未确认时进入后续阶段。失败状态会保留错误信息，便于用户修复输入或网络问题后重新执行对应步骤。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/out/release_state_flow.png}
    \caption{创作者发行状态流转图}
    \label{fig:release_state_flow}
\end{figure}


\section{IPFS素材上传与metadata模块实现}

创作者选择音频和封面后，桌面端将文件提交给后端Pinata上传接口。后端读取文件内容，调用Pinata API上传，并把返回CID、文件大小、MIME类型和Pinata ID记录到数据库。

metadata JSON由桌面端根据作品信息、音频CID、封面CID、访问模式、价格、链上合约地址和歌词信息生成。metadata上传成功后，系统将metadataStorageObjectId和metadataDocument回写到发行记录中。

\begin{table}[htbp]
    \linespread{1.5}
    \zihao{5}
    \songti
    \centering
    \caption{metadata JSON主要字段说明}\label{tab:metadata_json_fields}
    \begin{tabular}{|l|p{9.5cm}|}
        \hline
        字段             & 说明                                                        \\ \hline
        name            & 作品标题，用于链上tokenURI对应metadata的基础展示                           \\ \hline
        description     & 作品描述，来源于创作者工作台编辑内容                                      \\ \hline
        image           & 封面gateway地址或IPFS引用，用于详情页和音乐库封面展示                          \\ \hline
        animation\_url  & 音频gateway地址或IPFS引用，用于播放器解析远程音频资源                           \\ \hline
        attributes      & 结构化扩展属性，可包含艺术家、专辑、流派、访问模式、试听秒数和链上合约信息等                \\ \hline
        external\_url   & 作品外部链接，可指向后端资源详情或区块浏览器页面                               \\ \hline
        freeflow        & FreeFlow扩展字段，可保存audioCid、coverCid、metadataCid、访问模式和来源信息等 \\ \hline
    \end{tabular}
\end{table}


\section{链上发布与收益分账模块实现}

metadata准备完成后，创作者可调用PlatformHub合约的publishTrack方法发布作品。该方法内部创建收益分账合约、创建音乐访问凭证tokenId，并保存作品的销售配置。交易确认后，桌面端解析TrackPublished事件，获取tokenId、creator和splitter地址，并将交易哈希、区块号和tokenId回写后端。

收益分账由RoyaltySplitter合约完成。创作者可配置多个收款地址及其份额，购买收益进入分账合约后，各收款人可按份额提取。


\section{链上音乐详情、购买与访问验证模块实现}

用户在搜索结果中打开FreeFlow链上音乐详情页后，系统根据resourceKey解析后端资源索引，并读取链上销售配置。详情页会展示访问状态、价格、创作者地址、metadata链接和发布交易链接。

对于公开作品，用户可直接加入链上音乐库。对于付费作品，系统调用PlatformHub的buyAccess方法完成购买，购买成功后记录purchase，并再次查询ERC-1155余额确认访问凭证状态。

该模块的访问判断遵循“链上结果优先”的原则。后端资源索引用于定位作品和展示元数据，但付费访问状态不由后端单独决定，而是由PlatformHub的\texttt{hasAccess}和MusicAccess1155的余额结果共同支撑。购买成功后写入\texttt{purchases}表，是为了后续展示购买历史、排序热度和降低重复写入风险。


\section{播放器、音频代理与链上音乐库模块实现}

FreeFlow使用桌面端音频代理统一处理播放请求。播放器请求本地代理接口后，代理服务优先读取本地文件，其次读取磁盘缓存，最后从FreeFlowProvider解析到的IPFS gateway地址拉取音频流。代理服务支持Range请求，因此用户可以拖动进度条进行分段播放。

音频代理与缓存流程如图\ref{fig:audio_proxy_cache_flow}所示。该流程将本地文件、缓存文件和远程音频源统一封装为\texttt{/proxy}响应，降低渲染进程直接处理跨域、Range和缓存细节的复杂度。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/out/audio_proxy_cache_flow.png}
    \caption{音频代理与缓存流程图}
    \label{fig:audio_proxy_cache_flow}
\end{figure}

链上音乐库用于管理公开或已购买的链上资源。用户将资源加入链上音乐库后，系统会在本地数据库中保存TrackEntity及其freeflow字段，后续可以进行播放、下载和管理。


\section{评论与用户互动模块实现}

评论模块由后端comments模块和桌面端CommentView共同实现。用户登录后可以针对某个releaseId发表评论，后端保存评论内容、用户ID和时间信息。桌面端展示评论列表、作者信息和发布时间，并在用户未登录时提示返回Profile引导完成登录。


\section{本章小结}

本章按照系统功能模块介绍了FreeFlow核心实现。各模块围绕钱包身份、链上发行、IPFS内容寻址、合约访问控制和桌面端播放体验展开，形成从创作者发布到用户检索、购买、播放和互动的主要业务闭环。
